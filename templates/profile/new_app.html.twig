{% extends 'base.html.twig' %}

{% block title %}Add New Application{% endblock %}

{% block body %}
    <div>
        <h1>Add New Application</h1>
        <div>
            {{ form_start(addApplicationForm) }}
            {{ form_row(addApplicationForm.name) }}
            {{ form_row(addApplicationForm.description) }}

            <button type="submit">Add Application</button>
            {{ form_end(addApplicationForm) }}

            <a href="{{ path('app_profile') }}">Back</a>
        </div>
    </div>
{% endblock %}
